# FurlPay API key. Omit to run the tools in demo mode (deterministic sample data,
# writes simulate without touching the network).
FURLPAY_API_KEY=fp_live_sk_...

# Optional: point at the sandbox instead of production.
# FURLPAY_API_BASE=https://sandbox.api.furlpay.com/v1

# Needed only for examples/agent.py (the LLM step).
# ANTHROPIC_API_KEY=sk-ant-...
